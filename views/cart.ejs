<%- include('./partials/header') %>

<div class="flex flex-col lg:flex-row gap-8 px-4 sm:px-6 lg:px-10 py-10 bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">

    <!-- LEFT: Cart Items -->
    <div class="w-full lg:w-2/3 space-y-6">
        <div class="flex items-center justify-between border-b-2 border-gray-300 pb-4">
            <h2 class="text-3xl font-bold text-gray-800">Shopping Cart</h2>
            <span class="text-sm text-gray-600"><%= user.cart.filter(item => item.product).length %> Items</span>
        </div>

        <% if (user.cart.length === 0) { %>
            <div class="bg-white rounded-2xl shadow-lg p-16 text-center">
                <svg class="w-24 h-24 mx-auto text-gray-300 mb-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
                </svg>
                <h3 class="text-2xl font-semibold text-gray-700 mb-2">Your cart is empty</h3>
                <p class="text-gray-500 mb-6">Add some products to get started!</p>
                <a href="/shop" class="inline-block bg-black text-white px-8 py-3 rounded-full hover:bg-gray-800 transition-all transform hover:scale-105">
                    Continue Shopping
                </a>
            </div>
        <% } else { %>
            <% user.cart.forEach(function(item, index) { %>
                <% if (item.product) { %>
                <div class="group bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-100">
                    <div class="flex flex-col sm:flex-row">
                        <!-- Image -->
                        <div class="w-full sm:w-1/3 bg-gradient-to-br from-gray-50 to-gray-100 flex items-center justify-center p-6 relative">
                            <% if (item.product.image) { %>
                                <img class="h-48 w-full object-contain transform group-hover:scale-105 transition-transform duration-300" 
                                     src="data:image/jpeg;base64,<%= item.product.image.toString('base64') %>" 
                                     alt="<%= item.product.name %>">
                            <% } else { %>
                                <div class="text-gray-300 text-center">
                                    <svg class="w-20 h-20 mx-auto mb-2" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd"></path>
                                    </svg>
                                    <span class="text-sm">No image</span>
                                </div>
                            <% } %>
                            <!-- Discount Badge -->
                            <% if (item.product.originalPrice > item.product.price) { %>
                                <div class="absolute top-4 left-4 bg-red-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                    <%= Math.round(((item.product.originalPrice - item.product.price) / item.product.originalPrice) * 100) %>% OFF
                                </div>
                            <% } %>
                        </div>

                        <!-- Info -->
                        <div class="w-full sm:w-2/3 p-6 flex flex-col justify-between">
                            <div>
                                <div class="flex justify-between items-start mb-3">
                                    <div>
                                        <h3 class="text-xl font-bold text-gray-800 mb-1 group-hover:text-blue-600 transition-colors">
                                            <%= item.product.name %>
                                        </h3>
                                        <p class="text-sm text-gray-500">Size: XS • Color: Default</p>
                                    </div>
                                    <form action="/cart/remove/<%= item.product._id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="text-gray-400 hover:text-red-500 transition-colors p-2">
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                            </svg>
                                        </button>
                                    </form>
                                </div>
                                
                                <div class="flex items-center gap-2 mb-4">
                                    <% if (item.product.originalPrice > item.product.price) { %>
                                        <span class="text-sm text-gray-400 line-through">₹<%= item.product.originalPrice.toLocaleString('en-IN') %></span>
                                    <% } %>
                                    <span class="text-2xl font-bold text-gray-900">₹<%= item.product.price.toLocaleString('en-IN') %></span>
                                </div>
                            </div>

                            <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                                <!-- Quantity Controls -->
                                <div class="flex items-center bg-gray-100 rounded-full p-1">
                                    <form action="/cart/decrease/<%= item.product._id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="w-9 h-9 rounded-full bg-white text-gray-700 font-bold flex items-center justify-center hover:bg-gray-200 transition-all shadow-sm hover:shadow">
                                            −
                                        </button>
                                    </form>
                                    <span class="text-lg font-semibold mx-4 min-w-[2rem] text-center"><%= item.quantity %></span>
                                    <form action="/cart/increase/<%= item.product._id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="w-9 h-9 rounded-full bg-white text-gray-700 font-bold flex items-center justify-center hover:bg-gray-200 transition-all shadow-sm hover:shadow">
                                            +
                                        </button>
                                    </form>
                                </div>
                                
                                <!-- Subtotal -->
                                <div class="text-right">
                                    <p class="text-xs text-gray-500 mb-1">Subtotal</p>
                                    <p class="text-xl font-bold text-gray-900">
                                        ₹<%= (item.product.price * item.quantity).toLocaleString('en-IN') %>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
            <% }) %>
        <% } %>
    </div>

    <!-- RIGHT: Order Summary -->
    <div class="w-full lg:w-1/3 h-fit">
        <div class="bg-white shadow-xl rounded-2xl p-6 sticky top-6 border border-gray-100">
            <h3 class="text-2xl font-bold border-b-2 border-gray-200 pb-4 mb-6">Order Summary</h3>

            <div class="space-y-4 text-sm mb-6">
                <div class="flex justify-between items-center">
                    <span class="text-gray-600">Subtotal (<%= user.cart.filter(item => item.product).reduce((acc, item) => acc + item.quantity, 0) %> items)</span>
                    <span class="font-semibold text-gray-900">₹<%= user.cart.filter(item => item.product).reduce((acc, item) => acc + item.product.price * item.quantity, 0).toLocaleString('en-IN') %></span>
                </div>
                
                <div class="flex justify-between items-center text-gray-400">
                    <span>Original Price</span>
                    <span class="line-through">₹<%= user.cart.filter(item => item.product).reduce((acc, item) => acc + item.product.originalPrice * item.quantity, 0).toLocaleString('en-IN') %></span>
                </div>
                
                <div class="flex justify-between items-center">
                    <span class="text-gray-600">Shipping</span>
                    <span class="text-green-600 font-semibold flex items-center gap-1">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                        </svg>
                        FREE
                    </span>
                </div>
                
                <div class="flex justify-between items-center bg-red-50 -mx-6 px-6 py-3 rounded-lg">
                    <span class="text-red-600 font-medium">You Save</span>
                    <span class="text-red-600 font-bold">₹<%= user.cart.filter(item => item.product).reduce((acc, item) => acc + (item.product.originalPrice - item.product.price) * item.quantity, 0).toLocaleString('en-IN') %></span>
                </div>
            </div>

            <!-- Total -->
            <div class="border-t-2 border-gray-200 pt-4 mb-6">
                <div class="flex justify-between items-center">
                    <span class="text-lg font-semibold text-gray-700">Total Amount</span>
                    <span class="text-3xl font-bold text-gray-900">₹<%= user.cart.filter(item => item.product).reduce((acc, item) => acc + item.product.price * item.quantity, 0).toLocaleString('en-IN') %></span>
                </div>
            </div>

            <!-- Promo Code -->
            <div class="mb-6">
                <label class="text-sm font-semibold text-gray-700 block mb-2">Have a promo code?</label>
                <form action="/cart/apply-promo" method="POST">
                    <div class="flex gap-2">
                        <input type="text" 
                               name="promoCode"
                               placeholder="Enter code" 
                               class="flex-1 border-2 border-gray-200 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 transition-colors">
                        <button type="submit" class="bg-gradient-to-r from-blue-500 to-blue-600 text-white px-6 py-2.5 rounded-lg hover:from-blue-600 hover:to-blue-700 transition-all font-medium shadow-sm hover:shadow">
                            Apply
                        </button>
                    </div>
                </form>
            </div>

            <!-- Checkout Button -->
            <a href="/checkout" class="block w-full bg-gradient-to-r from-black to-gray-800 text-white text-center py-4 rounded-xl hover:from-gray-800 hover:to-black transition-all text-base font-semibold tracking-wide shadow-lg hover:shadow-xl transform hover:scale-[1.02] active:scale-[0.98]">
                PROCEED TO CHECKOUT
            </a>

            <!-- Security Badge -->
            <div class="mt-6 flex items-center justify-center gap-2 text-xs text-gray-500">
                <svg class="w-4 h-4 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span>Secure Checkout • SSL Encrypted</span>
            </div>

            <!-- Payment Icons -->
            <div class="flex justify-center gap-4 mt-6 pt-6 border-t border-gray-100">
                <img src="https://upload.wikimedia.org/wikipedia/commons/4/41/Visa_Logo.png" class="h-6 opacity-60 hover:opacity-100 transition-opacity" alt="Visa">
                <img src="https://upload.wikimedia.org/wikipedia/commons/2/2a/Mastercard-logo.svg" class="h-6 opacity-60 hover:opacity-100 transition-opacity" alt="MasterCard">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/3/30/RuPay_Logo.svg/1280px-RuPay_Logo.svg.png" class="h-6 opacity-60 hover:opacity-100 transition-opacity" alt="RuPay">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/f/fc/UPI-Logo-vector.svg/2560px-UPI-Logo-vector.svg.png" class="h-6 opacity-60 hover:opacity-100 transition-opacity" alt="UPI">
            </div>
        </div>
    </div>

</div>

<%- include('./partials/footer') %>